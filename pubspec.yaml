name: school_soft_project
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  
  flutter_riverpod: ^1.0.4
  go_router: ^4.4.1
  
  uuid: ^3.0.6

  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

  firebase_core: ^1.23.0
  firebase_auth: ^3.10.0

  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  generate: true
  uses-material-design: true
